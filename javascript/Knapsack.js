/**
 * @param {Number} maxLimit Total number of weight allowed by the knapsackj
 * @param {Array} valuesArr Set of values - containing weights and values
 *
 * @returns
 */
function knapsack(valuesArr, maxLimit) {}
