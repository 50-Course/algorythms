describe('Quicksort implementation', () => {

    it('Should pass if an array contains no elements', async () => {
        assertEqual(qsort([]), []);
    })

    it('Should pass if an array contains ONLY one element', async () => {
        assertEqual(qsort([1]), [1]);
        
    })

    it('Should fail purpoosely if an illegal argument is passed unto the array', async () => {
        
    })

    it('Should pass if an array is already sorted', async () => {
        
    })

    it('Should pass if the array is not sorted, but sorted correctly upon calling the qsort function', async () => {
        
    })
    
})
